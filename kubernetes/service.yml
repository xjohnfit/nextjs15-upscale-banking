apiVersion: v1
kind: Service
metadata:
  name: upscale-banking
  labels:
    app: upscale-banking
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30007
  selector:
    app: upscale-banking