apiVersion: v1
kind: Service
metadata:
  name: upscale-banking
  labels:
    app: upscale-banking
spec:
  ports:
  - port: 3000
    targetPort: 3000
  selector:
    app: upscale-banking

  type: NodePort